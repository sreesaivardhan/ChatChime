// ─── Backend URL Configuration ────────────────────────────────────────────────
// In production: set this to your Render URL before deploying to Netlify.
// In local dev: leave as-is — falls back to localhost:3001.
//
// Example for production:
//   window.BACKEND_URL = 'https://chatchime-api.onrender.com';

window.BACKEND_URL = window.BACKEND_URL || 'http://localhost:3001';
