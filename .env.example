# Port for the backend server (Render sets this automatically)
PORT=3001

# The allowed origin for CORS (Express and Socket.IO).
# REQUIRED in production. Throw error if missing.
# Example for Render: https://your-app.netlify.app
# In local dev, it will default to http://localhost:3000 if not set.
FRONTEND_ORIGIN=http://localhost:3000
